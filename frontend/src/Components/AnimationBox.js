import { Box, styled } from "@mui/material";

const AnimationBox = styled(Box)({
  width: "6rem",
  height: "6rem",
  overflow: "hidden",
  borderRadius: "50%",
  position: "relative",
});
export default AnimationBox;
